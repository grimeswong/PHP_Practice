<?php
  // A useful array function is join(separator, array)
  // join() create a string containing all elements of the array separated by
  // the string speified in the first parameter

  $fruit = array('apple', 'orange', 'pineapple');
  echo join(', ', $fruit);
 ?>
